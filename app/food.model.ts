export class Food {
  // name: string;
  // detail: string;
  // calories: number;

  constructor(
    public name: string,
    public detail: string,
    public calories: number
  ) {
    this.name = name;
    this.detail = detail;
    this.calories = calories;
  }
}
